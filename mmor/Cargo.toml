[package]
name = "mmor"
description = """
mmor provides an OS-agnostic library for enumering memory-mapped objects
in the current process."""
version = "0.1.0"
edition = "2024"

[target.'cfg(unix)'.dependencies]
libc = "0.2.171"

[dependencies]
dlrkit = { version = "0.1.0", path = "../dlrkit" }
